route:
  receiver: operator-webhook
  group_by: [alertname]
  group_wait: 15s
  group_interval: 5m
  repeat_interval: 2h

receivers:
  - name: operator-webhook
    webhook_configs:
      - url: https://replace-with-operator-alert-endpoint.invalid/alerts
        send_resolved: true
